// android/build.gradle   (sqflite_sqlcipher)

def flutterRoot = rootProject.ext.flutterRoot
apply from: "$flutterRoot/packages/flutter_tools/gradle/flutter.gradle"

android {
    namespace "com.davidmartos96.sqflite_sqlcipher"   // o el que quieras
    compileSdkVersion 34

    defaultConfig {
        minSdkVersion 21
    }

    // Evita la advertencia de namespace duplicado en AGP 8+
    lintOptions {
        disable 'InvalidPackage'
    }
}

dependencies {
    // NADA más: flutter.gradle añade flutter.jar automáticamente
}